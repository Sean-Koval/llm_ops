# name: Build and Deploy to GKE

# on:
#   push:
#     branches: [ main ]
#   workflow_dispatch:

# env:
#   PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
#   GKE_CLUSTER: llm-ops-cluster
#   GKE_ZONE: us-central1-a
#   IMAGE: llm-ops-pipeline

# jobs:
#   setup-build-publish-deploy:
#     name: Setup, Build, Publish, and Deploy
#     runs-on: ubuntu-latest

#     steps:
#     - name: Checkout
#       uses: actions/checkout@v3

#     # Setup gcloud CLI
#     - name: Set up Cloud SDK
#       uses: google-github-actions/setup-gcloud@v1
#       with:
#         service_account_key: ${{ secrets.GCP_SA_KEY }}
#         project_id: ${{ secrets.GCP_PROJECT_ID }}
#         export_default_credentials: true

#     # Configure Docker to use the gcloud command-line tool as a credential helper
#     - name: Auth Docker
#       run: |
#         gcloud --quiet auth configure-docker
        
#     # Get the GKE credentials
#     - name: Set up GKE credentials
#       uses: google-github-actions/get-gke-credentials@v1
#       with:
#         cluster_name: ${{ env.GKE_CLUSTER }}
#         location: ${{ env.GKE_ZONE }}
#         credentials: ${{ secrets.GCP_SA_KEY }}

#     # Build the Docker image
#     - name: Build
#       run: |
#         docker build -t gcr.io/$PROJECT_ID/$IMAGE:$GITHUB_SHA \
#           --build-arg GITHUB_SHA="$GITHUB_SHA" \
#           --build-arg GITHUB_REF="$GITHUB_REF" .

#     # Push the Docker image to Google Container Registry
#     - name: Publish
#       run: |
#         docker push gcr.io/$PROJECT_ID/$IMAGE:$GITHUB_SHA
        
#     # Update deployment manifests
#     - name: Update kustomization
#       run: |
#         # Update the image name in the deployment file
#         sed -i "s|gcr.io/YOUR_PROJECT_ID/llm-ops-pipeline:latest|gcr.io/$PROJECT_ID/$IMAGE:$GITHUB_SHA|g" k8s/api-deployment.yaml

#     # Deploy the app to the GKE cluster
#     - name: Deploy
#       run: |
#         kubectl apply -f k8s/volumes.yaml
#         kubectl apply -f k8s/api-deployment.yaml
#         kubectl apply -f k8s/mlflow-deployment.yaml
#         kubectl apply -f k8s/monitoring-deployment.yaml
        
#     # Display deployed services
#     - name: Get Services
#       run: |
#         echo "Deployed services:"
#         kubectl get services